[project]
name = "ninelives"
version = "0.1.0"
description = "An AI agent whose mind lives entirely in CockroachDB."
requires-python = ">=3.12"
dependencies = [
    "psycopg[binary]>=3.2",
    "anthropic>=0.84",
    "boto3>=1.35",
    "python-dotenv>=1.0",
    "sentence-transformers>=3.0",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "sse-starlette>=2.1",
    "pydantic>=2.9",
]

[tool.uv]
package = false
